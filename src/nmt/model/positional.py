"""Positional encodings: sinusoidal | learned | RoPE | ALiBi (config.pos_encoding).

Default sinusoidal. sinusoidal/learned add to the token embeddings; RoPE rotates
q/k inside attention; ALiBi adds a distance bias to attention scores. config.max_len
caps the table size.
"""

# TODO(rishi): implement
