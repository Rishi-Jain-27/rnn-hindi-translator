"""nmt.model — from-scratch Transformer (encoder-decoder) NMT.

Will re-export Transformer + build_model once transformer.py is implemented.
Kept import-free for now so `import nmt` works while the core is still stubs.
"""
